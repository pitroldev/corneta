// Chaves de feature — liga/desliga recursos inteiros sem apagar código.

/** Mesa (co-stream com convidados): escondida da UI até estar pronta pro público.
 *  Pra reativar, basta voltar pra `true` — sidebar, atalhos e navegação religam juntos. */
export const MESA_ENABLED = false;
